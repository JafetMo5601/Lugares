buildscript {

    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath 'com.google.gms:google-services:4.3.10'
        //Para poder pasar parámentros
        classpath 'androidx.navigation:navigation-safe-args-gradle-plugin:2.4.1'
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}